[tool.poetry]
name = "bscp-labs-randomizer"
version = "0.1.0"
description = ""
authors = ["Tijmen Desktop <tjakobsen@live.nl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
